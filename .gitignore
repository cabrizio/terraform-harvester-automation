# Create a .gitignore for the terraform project and ansible project
# Ignore files generated by Terraform

# Local .terraform directories
**/.terraform/*
# .tfstate files
*.tfstate
*.tfstate.*
# Crash log files
crash.log
# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars
# Override example .tfvars files
!example.tfvars
# Ignore override files as they are usually used to override resources locally and so don't need to be
override.tf
override.tf.json
# Include override files that end with _example.tf as an example
!override_example.tf
!override_example.tf.json
# Ignore CLI configuration files
.terraformrc
terraform.rc
# Ignore any .env files that may contain sensitive data
.env
.env.*.local
# Ignore any .secrets files that may contain sensitive data
.secrets
.secrets.*.local
# Ignore any .auto.tfvars files that may contain sensitive data
*.auto.tfvars
*.auto.tfvars.json
# Ignore any .backup files that may contain sensitive data
*.backup
# Ignore any .log files that may contain sensitive data
*.log
# Ignore any .pem files that may contain sensitive data
*.pem
# Ignore any .key files that may contain sensitive data
*.key
# Ignore any .crt files that may contain sensitive data
*.crt
# Ignore any .cer files that may contain sensitive data
*.cer
# Ignore any .pfx files that may contain sensitive data
*.pfx
# Ignore any .p12 files that may contain sensitive data
*.p12

# Ignore files generated by Ansible
# Ignore Ansible inventory files that may contain sensitive data
inventory.ini
# Ignore Ansible vault files that may contain sensitive data
*.vault
# Ignore Ansible log files
ansible.log
# Ignore Ansible retry files
*.retry
# Ignore Ansible fact cache files
fact_cache/
# Ignore Ansible temporary files
*.retry
*.tmp
*.temp
.terraform.lock.hcl
.ansible_password

#python ignore file
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
.venv/
ENV/
env.bak/
venv.bak/
ansible/demo-key
ansible.cfg
.env.local
config.sh
secrets.sh
credentials.sh

# Shell script temporary files
*.bak
.*.sw?

# Shell history (if accidentally created in project)
.bash_history
.zsh_history
